import React from "react";
import * as ReactDOM from "react-dom";
import { {{pascalCaseName}} } from "./{{dashedName}}";

test("renders {{pascalCaseName}}", () => {
  ReactDOM.render(<{{pascalCaseName}} />);
});
